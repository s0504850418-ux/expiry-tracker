{
  "name": "expiry-tracker-functions",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:watch": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test scripts",
    "test": "vitest run",
    "bootstrap": "tsx scripts/bootstrapBusiness.ts"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^24.13.2",
    "oxlint": "^1.71.0",
    "tsx": "^4.20.6",
    "typescript": "~5.9.3",
    "vitest": "^3.2.5"
  }
}
