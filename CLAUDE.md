# הקשר לפרויקט — קרא לפני שמתחילים

קובץ זה נטען אוטומטית על ידי Claude Code בתחילת כל שיחה. **חשוב:** קלוד קוד (כאן, ב-VS Code) וקלוד ב-claude.ai (בדפדפן) הם שני מוצרים נפרדים לגמרי בלי זיכרון משותף — כל מה שסוכם בשיחה בדפדפן חייב להיות כתוב כאן או בקבצי הפרויקט (README.md, DATA_MODEL.md) כדי שיהיה ידוע כאן.

## מה המערכת

מעקב דיגיטלי אחר תאריכי תפוגה של מוצרים מוכנים במטבח מסעדה (כגון רטבים), במקום מדבקות ידניות. פיילוט במסעדה של אשת בעל הפרויקט. מטרות: מניעת הגשת מוצרים פגי תוקף, עבודה לפי FEFO, דיוק כמויות ייצור עתידיות, וחישוב שווי כספי מדויק של פחת.

בעל הפרויקט הוא סטודנט להנדסת תוכנה (Full Stack: JS/TS/C#/SQL/Angular/Python/Node/MongoDB), **לא כותב קוד בעצמו בפרויקט הזה** — מבקש שהכל ייבנה עבורו, אבל מבין קוד ורוצה הסברים ברורים, לא רק "זה מוכן". לא לוותר על בדיקות אמיתיות ולא לסמן שלב כגמור בלי שהוא רץ בפועל.

## כלל דיווח מיידי על סטייה מהחלטה נעולה

**כל שינוי בקוד שסוטה בפועל מהחלטה המתועדת כ"נעולה" בסעיף הבא — בין אם זו סטייה מכוונת (כי המימוש בפועל דורש את זה) ובין אם היא תוצאת לוואי לא-מכוונת של שינוי אחר — חייב להיות מדווח לבעל הפרויקט במפורש, באותו רגע שבו הוא מתגלה או מבוצע.** לא מספיק לתעד את זה "איפשהו" בדוח סיום שלב או להסתמך על כך שסקירה עצמית מאוחרת תגלה את זה. **סיבה**: בשלבים 9–14 יכולות מנהל/ת המשמרת הורחבו בהדרגה (הוספת מוצר/מרכיב, הגדרת מתכון) בניגוד ישיר להחלטה הנעולה "מנהל משמרת לא יכול להוסיף מוצר/מרכיב חדש" — בלי שזה דווח אף פעם כסתירה, עד שסקירה עצמית בשלב 11 גילתה את זה בדיעבד, חודשים אחרי שהקוד כבר סטה. **איך להפעיל**: לפני שמסמנים שלב/משימה כגמורים, ולפני כל commit שנוגע בהתנהגות המתוארת ב"החלטות מוצר/טכניות נעולות" למטה — לעצור ולבדוק אם ההתנהגות בפועל עדיין תואמת את הניסוח הנעול. אם לא — לדווח את הפער במפורש בתשובה לבעל הפרויקט (לא רק בקובץ), ולשאול אם הסטייה מאושרת לפני שממשיכים, אלא אם כן היא כבר אושרה מפורשות בשיחה הנוכחית.

## החלטות מוצר נעולות (מהאפיון המלא — אין לשנות בלי לציין זאת במפורש)

- **הרשאות דו-שכבתיות:** בעל/ת העסק (קוד מנהל, גישה מלאה כולל דוחות כספיים ורווחיות) מול מנהל/ת משמרת (PIN אישי, מוגבל לטאבלט בלבד — פתיחת אצוות מוצרים קיימים וסימונן כטופלו/פג תוקף/הושלכו). מנהל משמרת לא יכול להוסיף מוצר/מרכיב חדש, לשנות מחיר או חיי מדף.
- **אצווה (Batch) היא הישות המרכזית** — לא "מוצר". כל הכנה יוצרת אצווה עם מזהה ייחודי; כל הדוחות, ההדפסות, הסריקות וההיסטוריה מתייחסים לאצווה.
- **שם מוצר ייחודי** בתוך העסק. אין המרה בין יחידות מידה (ק"ג/ליטר/יחידה) — כל מוצר קבוע ביחידה אחת.
- **FEFO** (לא FIFO) — המערכת תמיד ממליצה על האצווה שתפוג ראשונה.
- **QR מכיל רק batchId**, שאר הפרטים נשלפים בזמן סריקה כדי שיהיו תמיד עדכניים.
- **גרסאות מתכון**: שינוי מתכון יוצר גרסה חדשה; אצווה שומרת הפניה לגרסה שבה נעשתה, כולל תמונת מצב (snapshot) של מחירי המרכיבים באותו רגע. עדכון מחיר משפיע רק על אצוות/גרסאות עתידיות.
- **שני שדות זמן בכל אצווה**: `preparedAtClient` (מה שהעובד הזין) ו-`preparedAtServer` — כדי שניתוק אינטרנט זמני לא יעוות את התאריך האמיתי.
- **אין מחיקה לעולם** — הכל עובר סטטוסים (active/used/expired/discarded/archived, active:false למוצר שהופסק). ארכוב טכני של אצוות מגיל כשנה, לביצועים בלבד, לא למחיקה.
- **טיפול בכשל הדפסה**: יצירת אצווה תמיד כרוכה בניסיון הדפסה; אם נכשל — לא הופכת אצווה לפעילה בשקט, מוצגת שגיאה עם אפשרות ניסיון חוזר.
- **שימוש חלקי באצווה**: ברירת מחדל — עדכון כמות בסוף חיי האצווה בלבד. ניתן להגדיר לכל מוצר תדירות עדכון שונה (למשל כל סוף יום).
- **חיפוש מוצרים/אצוות לפי שם** — נכלל כבר בגרסה הראשונה.
- **סיבת פחת מרשימה סגורה וקבועה מראש** — כשמסמנים אצווה כהושלכה, בנוסף לכמות יש לבחור סיבה מתוך רשימה סגורה (למשל: פג תוקף, הכנת יתר, בעיית איכות, נפילה/זיהום, טעות בהכנה, אחר) — **לא** שדה טקסט חופשי. המטרה: מהירות לעובד בזמן משמרת ונתונים אחידים לדוח.
- **הרשימה הראשית בטאבלט מציגה רק שם מוצר וזמן שנותר עד לתפוגה.** כמות ויחידה נשמרות במסד הנתונים לצורך דוחות, אבל **לא** מוצגות בשורה הראשית — כדי לשמור את המסך נקי וממוקד לקריאה מהירה מהמטבח.
- **התראות בתוך המכשיר בלבד** (לא וואטסאפ/SMS), עם תזכורת חוזרת כל כמה שעות אם לא טופלה. **זמן ההתראה (כמה זמן לפני התפוגה) ניתן להגדרה לכל מוצר בנפרד** (למשל יום או יומיים לפני, תלוי כמה מהר המוצר מתקלקל/כמה זמן לוקח להחליט מה לעשות איתו) — **לא ערך גלובלי קבוע** אחיד לכל המוצרים — עם אפשרות לבעל/ת העסק לקבוע/לשנות את הערך לכל מוצר (הגדרה owner-only, כמו חיי מדף ומחיר).
- כל פעולה רגישה נרשמת ב-**Audit Log** בלתי ניתן לעריכה מצד הלקוח, כולל זיהוי מכשיר.

מפורט במלואו ב-`DATA_MODEL.md` (מבנה Firestore) ובמסמך האפיון המלא שנמצא אצל בעל הפרויקט (לא בריפו).

## החלטות טכניות נעולות

- **React + TypeScript + Vite**, **PWA** (`vite-plugin-pwa`) — לא native app, לא חנות אפליקציות.
- **Firebase**: Firestore + Hosting + Cloud Functions + Authentication. שני פרויקטי Firebase נפרדים (dev/prod), ראה `.firebaserc` (עדיין placeholders — טרם נוצרו פרויקטים אמיתיים).
- **טאבלט אנדרואיד דווקא** (לא iPad) — כדי לתמוך ב-Web Bluetooth מול מדפסת המדבקות.
- **פעולות רגישות אף פעם לא נכתבות ישירות מהלקוח** — עוברות דרך Cloud Functions שמאמתות קוד PIN/מנהל מול bcrypt hash בצד השרת, ומבצעות את הכתיבה באמצעות Admin SDK (שעוקף את Firestore Rules). Firestore Rules חוסמות כתיבה ישירה מהלקוח לכל מה שרגיש.
- **מדפסת מדבקות תרמית** — דגם עדיין לא נבחר/נרכש. אין להניח תאימות לדגם ספציפי; לבנות שכבת Printer Adapter.
- **עבודה אופליין** דרך Firestore persistence מובנה.

## מגבלות ידועות בסביבת הפיתוח הקודמת (claude.ai)

- נבנה ונבדק שם שלד ראשוני נפרד עם Cloud Functions + Firestore Rules + בדיקות יחידה (`tests/logic.test.js`, 8/8 עברו). בדיקות ה-Rules עצמן (`tests/rules.test.js`) **נכתבו אך לא הורצו בפועל** — הסביבה שם לא הצליחה להוריד את Firestore Emulator בגלל הגבלת רשת. יש להריץ אותן כאן בפועל לפני שממשיכים לסמוך עליהן:
  ```
  npm install -D firebase-tools @firebase/rules-unit-testing
  npx firebase emulators:exec --only firestore --project demo-expiry-tracker "node tests/rules.test.js"
  ```
- הריפו הזה (`expiry-tracker/`) הוא שלד React+TS+PWA **נפרד ונקי** שנבנה לפי תוכנית שלבים מסודרת (ראו למטה). ה-Cloud Functions/Rules/tests מהסביבה הקודמת **טרם שולבו לתוכו** — זה מתוכנן לשלב 2.

## תוכנית השלבים (כל שלב = ענף נפרד + PR נפרד, לא למזג ל-main בלי אישור)

1. ✅ הקמת פרויקט (React+TS+PWA, CI, סביבות dev/prod) — ענף `setup/project-foundation`, PR #1.
2. ✅ מודל נתונים ואבטחה (multi-tenant, הרשאות, PIN דרך Cloud Function, Firestore Rules + בדיקות אמיתיות מול Emulator) — `feature/auth-and-permissions`, PR #2.
3. ✅ מסך הטאבלט (רשימה לפי FEFO, חיפוש, יצירת אצווה, סימון סטטוס, כמות/סיבת פחת) — `feature/tablet-dashboard`, PR #3.
4. ✅ מוצרים/מרכיבים/מתכונים ועלויות — `feature/recipes-and-costs`, PR #4.
5. ✅ QR והדפסה (Printer Adapter, טיפול בכשל, הדפסה חוזרת, סריקה) — `feature/qr-and-printing`, PR #5.
6. ✅ עבודה אופליין (Firestore persistence, מניעת שליחה כפולה, טיפול בהתנגשויות) — `feature/offline-sync`, PR #6.
7. ✅ מסך ניהול (Google login, דוח פחת ורווחיות, יומן פעולות, PDF) — `feature/admin-dashboard`, PR #7.
8. ✅ בדיקות מקיפות + הכנה לפריסה (regression מלא + מסע E2E משולב על פני כל השלבים + עדכון README/DATA_MODEL) — `feature/comprehensive-testing`, PR #8.
9. ✅ מערכת התראות בתוך המכשיר — `feature/expiry-notifications`, PR #9. Scheduled Cloud Function (`checkExpiringBatches`) + `NotificationsPanel` בטאבלט + תזכורת חוזרת + סימון "טופל" אוטומטי. עודכן (2026-08-03): זמן ההתראה per-product (`notifyBeforeExpiryMinutes`) היה מוגדר בסכמה ונחשף בטופס המוצר אבל **לא נקרא בפועל** ע"י `processBusiness` — הפונקציה השתמשה בקבוע גלובלי קבוע לכולם, בניגוד ישיר להחלטה הנעולה. תוקן: `processBusiness` שולפת את כל המוצרים ובונה מפה productId→notifyBeforeExpiryMinutes, ומשתמשת בערך הספציפי (או בברירת המחדל הגלובלית אם לא הוגדר) לכל אצווה. גם הוסרה הסינון המוקדם לפי סף גלובלי בשאילתת ה-Firestore (היה עלול לפספס בשקט אצוות של מוצר עם חלון-התראה ארוך מהגלובלי) — נבדק תקן אמיתי מול Emulator: שני מוצרים עם אותו זמן-עד-תפוגה בדיוק, אחד עם ברירת מחדל (120 דק') ואחד עם חלון מותאם (300 דק') — רק השני קיבל התראה. **פער נפרד, עדיין פתוח:** ה-Scheduled Function (`onSchedule`) עצמה מעולם לא נבדקה/רצה דרך מנגנון ה-Cloud Scheduler האמיתי — לא כאן (Pub/Sub Emulator לא עולה על Windows) ולא בייצור (אין עדיין פרויקט Firebase אמיתי). כל הבדיקות עד כה קוראות ל-`processBusiness` ישירות. ראו "מה עדיין חסר" למטה.
10. ✅ שלב עיצוב מלא (חדש, לא היה בתוכנית המקורית) — כל המסכים (טאבלט + `/admin`) היו HTML גולמי בלי שום עבודת עיצוב. נבנתה מערכת עיצוב ב-CSS טהור (משתני `:root`, בלי ספריית UI חיצונית — כדי לא לנפח את גודל החבילה שכבר מעל מגה-בייט): כפתורים/שדות בגודל מגע נדיב (~52px), ניגודיות גבוהה, טיפוגרפיה קריאה ממרחק (בסיס 18px), תגי דחיפות (`urgency-badge`) לצד צבע הגבול (לא מסתמכים על צבע בלבד), `Spinner` משותף בכל כפתור async (טעינה), טוסט הצלחה חולף בטאבלט אחרי יצירת/עדכון אצווה, באנר שגיאה עקבי (`error-text`/`success-text`) בכל המסכים, טבלאות עם `table-scroll` (גלילה אופקית ברוחב טאבלט), ותג "סביבת פיתוח" (`EnvBadge`, לפי `VITE_APP_ENV`) שה-README כבר טען (בטעות) שקיים. אומת בדפדפן אמיתי (Playwright) ברוחב דסקטופ (1440px), רוחב טאבלט לרוחב (1280px) ולאורך (800px) — ראו PR של שלב זה לצילומי מסך.
11. **סקירה עצמית (2026-08-03) — עוד שתי החלטות נעולות שלא מומשו, נמצאו באותה שיטה שגילתה את פער ההתראות בשלב 8.** לא תוקנו בשלב הזה (רק דווחו, לפי ההנחיה) — ראו "מה עדיין חסר" למטה לפירוט המלא:
   - **"תדירות עדכון כמות בשימוש חלקי" (`partialUsageUpdateFrequency`) — ערך "כל סוף יום" מומש 0%.** ברירת המחדל ("בסוף חיי האצווה") *כן* עובדת בפועל — זה בדיוק מה ש-`updateBatchStatus` כבר עושה (מעדכן כמות רק כשסטטוס עובר לסופי). אבל **אין שום מנגנון** לעדכן כמות של אצווה שנשארת `active` (כל התדירות "כל סוף יום" נשענת על יכולת שלא קיימת) — `updateBatchStatus` מקבל רק `newStatus` מתוך `used`/`expired`/`discarded`, ואין endpoint אחר. השדה נשלף ב-`TabletDashboard` ומאוחסן אבל אף פעם לא נקרא שוב שם.
   - **ארכוב טכני של אצוות מגיל שנה — לא קיים כלל.** `archivedAt` נכתב כ-`null` ב-`createBatch` ולעולם לא מתעדכן — אין Scheduled Function שמארכבת. עדיפות נמוכה בפועל (לביצועים בלבד, לא לפיילוט חדש בלי היסטוריה של שנה), בניגוד לפער ההתראות/עדכון-כמות שהם התנהגות יומיומית מהיום הראשון.
   - `DATA_MODEL.md` תוקן: הסעיף על `notifications` עדיין תיאר את הלוגיקה כ"תיבנה בשלב מאוחר יותר" — עודכן לשקף ששלב 9 בנה אותה בפועל.
12. ✅ (2026-08-03) מימוש מלא של עדכון כמות באמצע חיי אצווה — סוגר את הפער הראשון שדווח בשלב 11 (`partialUsageUpdateFrequency: "endOfDay"` היה מוגדר בטופס המוצר בלי שום מנגנון מאחוריו).
    - **Cloud Function חדשה `updateBatchQuantity`** (`functions/src/batches/updateBatchQuantity.ts`) — מעדכנת `quantity` על אצווה שעדיין `active` (לא רק במעבר לסטטוס סופי כמו `updateBatchStatus`). דורשת חברות בעסק (owner/shiftManager — אותו דפוס הרשאה כמו כל שאר הפעולות בטאבלט: האימות מול PIN קורה פעם אחת בהתחברות דרך `verifyStaffPin`, לא בכל קריאה), מוודאת בתוך טרנזקציה ש-0 ≤ quantity ≤ preparedQuantity ושמצב האצווה עדיין active, ורושמת `batch.quantityUpdated` ל-Audit Log. **לא שומרת היסטוריית עדכונים** — רק את הכמות הנוכחית (`quantity`) ואת זמן העדכון האחרון (`quantityLastUpdatedAt`), כפי שנדרש: "מספיק למטרה של מה יש עכשיו במקרר".
    - **`preparedQuantity` ממשיך לא להשתנות לעולם** — נשאר קבוע מרגע היצירה. דוח הפחת (`WasteReport.tsx`) ממשיך לחשב wasteRatio = quantity/preparedQuantity **בזמן ההשלכה הסופית בלבד**, כך שהשווי הכספי של הפחת משקף את מה שבאמת הושלך (למשל 5→3→1→השלכת 1: הפחת הוא 1/5, לא ההפרש 5-1), בלי שום שינוי נדרש בקוד הדוח עצמו.
    - **UI בטאבלט** (`BatchRow.tsx`): כפתור "עדכון כמות" בשורת האצווה פותח טופס מוטמע קטן (`quantity-update-form`) בתוך אותה שורה — לא דיאלוג modal נפרד — עם שדה מספרי שמאותחל לכמות הנוכחית ואישור/ביטול. שומר על העיקרון הנעול "הרשימה הראשית מציגה רק שם וזמן שנותר": הכמות עצמה מוצגת רק כשלוחצים לערוך, לא כתצוגה פסיבית בשורה.
    - **תזכורת ויזואלית ל-endOfDay**: שדה חדש `quantityLastUpdatedAt` (Timestamp) על כל אצווה — נכתב ביצירה (`createBatch`) ומתעדכן ב-`updateBatchQuantity` בלבד (לא ב-`updateBatchPrintStatus`, כדי שניסיון הדפסה חוזר לא "יאפס" בטעות את התזכורת). לוגיקה טהורה חדשה `src/lib/quantityReminder.ts` (`needsDailyQuantityUpdate`) בודקת אם המוצר מוגדר **במפורש** ל-"endOfDay" וה-`quantityLastUpdatedAt` של האצווה אינו מהיום המקומי הנוכחי — אם כן, `BatchRow` מציגה תג "יש לעדכן כמות היום" (סימון ברשימה בלבד, **לא** רשומת התראה חוצה-מכשירים ב-collection `notifications`). מוצר בברירת מחדל (`null`/`endOfBatchLife`) לעולם לא מציג את התג.
    - **פער שנשאר פתוח במפורש (לא תוקן כאן, לא התבקש בשלב הזה):** `businesses/{businessId}.settings.defaultPartialUsageUpdateFrequency` (ברירת המחדל *ברמת העסק* לכשה-`partialUsageUpdateFrequency` של המוצר הוא null) עדיין לא נקרא על ידי שום קוד — התזכורת בודקת רק `partialUsageUpdateFrequency === "endOfDay"` **מפורש ברמת המוצר**. אם בעל/ת העסק ירצה/תרצה להגדיר "כל סוף יום" כברירת מחדל לכל העסק (ולא מוצר-מוצר), זה עדיין לא יעבוד. ראו `DATA_MODEL.md`.
    - נבדק: `test:functions`/`test:rules` (רגרסיה מלאה, כולל 3 בדיקות אינטגרציה חדשות ל-`updateBatchQuantity` מול Emulator אמיתי: עדכון תקין רציף 5→3→1, דחיית כמות שלילית/גדולה מ-preparedQuantity/עדכון על אצווה לא-פעילה, ואימות שהדוח הכספי מבוסס על הכמות שנזרקה בפועל) + אימות בדפדפן אמיתי.
    - **שני באגים אמיתיים נוספים שהמשתמש גילה בעצמו תוך כדי בדיקה ידנית בדפדפן במקביל לאימות הזה** (לא קשורים ישירות לעדכון כמות, אבל תוקנו באותו שלב לפי בקשתו המפורשת):
      1. **הודעות שגיאה ניחשו סיבה במקום לדווח את מה שבאמת קרה** — לדוגמה, טופס "מוצר חדש" הציג "השמירה נכשלה — ייתכן ששם המוצר כבר קיים" על **כל** כשל, כולל ניתוק מוחלט מהשרת (Emulator כבוי / `ERR_CONNECTION_REFUSED`), מבלי ששם המוצר בכלל נבדק. תוקן: `src/lib/describeError.ts` (חדש) ממפה קוד שגיאה *בפועל* (`functions/already-exists`, `not-found`, `permission-denied`, `invalid-argument`, `failed-precondition`, `resource-exhausted` וכו') להודעה מדויקת, עם `overrides` לניסוח ספציפי-להקשר בכל טופס/דיאלוג (`ProductFormDialog`, `IngredientFormDialog`, `StaffFormDialog`, `TeamManagement`, `ManageAdminAccess`, `DiscardReasonDialog`, `CreateBatchDialog`, `BatchRow`, `TabletDashboard`, `RecipeEditorDialog`, `WasteReport`, וגם רופקטור ל-`LoginScreen`/`AdminLoginScreen` שכבר עשו דבר דומה מקומית). קודי `internal`/`unavailable`/ללא-קוד-בכלל (שזה בדיוק מה שניתוק רשת/Emulator כבוי מחזיר מה-SDK של Firebase — ואין דרך להבחין ביניהם ברמת ה-SDK עצמו) מקבלים הודעה כנה ("אין חיבור לשרת, או שגיאה לא צפויה בצד השרת") **במקום** ניחוש ספציפי שגוי. נבדק בדפדפן אמיתי עם חסימת רשת אמיתית (Playwright route-abort לכתובת ה-Functions Emulator, בדפדפן מבודד — לא נגע ב-Emulator המשותף שהמשתמש עצמו בדק מולו במקביל): ההודעה שהתקבלה היא ההודעה הכנה, לא "השם כבר קיים".
      2. **מוצר חדש נוצר בלי שום דרך להזין מתכון**, כך שנשאר בלי עלות ובלי שווי פחת בדוח — בלי שהמשתמש/ת מבין/ה שמשהו חסר. תוקן: יצירת מוצר חדש (לא עריכת קיים) פותחת **אוטומטית** את עורך המתכון (`RecipeEditorDialog`) מיד אחרי השמירה (`ProductFormDialog.onSaved` מעביר את `productId` שנוצר בחזרה ל-`ProductsManagement`), וההודעה "אין עדיין מתכון למוצר זה" הורחבה להסביר את ההשלכה הכספית במפורש ("שווי הפחת שלו **לא יחושב** בדוח... אין עלות ידועה לייחס אליה"). אפשר עדיין לדלג ("סגירה") ולהוסיף מתכון מאוחר יותר. נבדק בדפדפן אמיתי (Playwright): יצירת מוצר → עורך מתכון נפתח אוטומטית → הוספת שורת מתכון → עלות מחושבת נכון (2 ק"ג × 5/ק"ג = 10.00).
    - **אימות בדפדפן אמיתי מקיף** (Playwright, headless, מול אותו Emulator stack): זרימת /admin עם Google (Auth Emulator fake-IDP), יצירת מרכיב+מוצר+מתכון, שני הבאגים שלמעלה, ואז בטאבלט — יצירת אצווה 5 ק"ג ← עדכון ל-3 ← עדכון ל-1 ← השלכה (הדיאלוג הציע כברירת מחדל 1, לא 5) ← ודוח הפחת הראה בדיוק 2.00 (1/5 × עלות 10) ולא 8.00 (חישוב שגוי לפי ההפרש 5-1) — 16/16 בדיקות עברו.
    - **סיבוב שני, לפי בקשת המשתמש: "בדוק גם את שאר הזרימות מנקודת מבט של משתמש חדש לגמרי, בלי שום נתונים במערכת".** בבדיקה בעצמו נתקע מיד: פתיחת עורך מתכון למוצר בעסק בלי אף מרכיב רשום הציגה "יש ליצור מרכיבים לפני יצירת מתכון" — משפט נכון עובדתית, אבל **ללא שום דרך להתקדם מתוכו** (אין קישור/כפתור, וסגירת הדיאלוג מחזירה בדיוק לאותה נקודה). תוקן:
      - **`RecipeEditorDialog` מכיל עכשיו כפתור "מרכיב חדש" מוטמע** שפותח את `IngredientFormDialog` כדיאלוג מוערם (stacked) בלי לצאת מעורך המתכון. `IngredientFormDialog.onSaved` מחזיר עכשיו גם את `ingredientId` שנוצר (כמו התיקון המקביל ב-`ProductFormDialog`), ו-`RecipeEditorDialog` בוחר אותו אוטומטית בשורת המתכון (הריקה, אם הייתה, אחרת שורה חדשה) — כדי שלא יהיה צריך לחפש אותו ברשימה בנפרד.
      - **עיצוב**: ההודעה "אין עדיין מתכון למוצר זה" ו"אין עדיין מרכיבים" עברו מ-`error-text` (אדום, "נכשל") ל-**`warning-text` חדש** (כתום/מידע, `src/index.css`) — כי שום דבר לא באמת נכשל שם, זה מצב התחלתי תקין; שימוש בעיצוב שגיאה למצב כזה מטעה ונראה כמו קריסה.
      - **תוקן גם דפוס דומה בטאבלט**: `TabletDashboard` הציג בעבר רק כפתור "אצווה חדשה" **מנוטרל בשקט** כשלעסק אין עדיין מוצרים, בלי שום הסבר — מנהל/ת משמרת (שאין לו/ה גישה בכלל ל-`/admin`) היה/הייתה נתקע/ת לגמרי בלי לדעת למה. עכשיו מוצגת הודעה (`warning-text`) שמסבירה את הסיבה, ומנוסחת לפי תפקיד המחובר/ת: לבעל/ת העסק — "יש להוסיף מוצר דרך מסך הניהול"; למנהל/ת משמרת — "יש לפנות לבעל/ת העסק".
      - נבדק בדפדפן אמיתי (Playwright): (1) מול `demoBiz` הקיים — עורך מתכון עם אפס מרכיבים פעילים מציג הודעה ניתנת-לפעולה בעיצוב warning, יצירת מרכיב חדש מתוך העורך עצמו (בלי לצאת ממנו) אוטומטית נבחר בשורת המתכון, וגרסת מתכון נשמרת בהצלחה (1.5 ק"ג × 4/ק"ג = 6.00). (2) מול עסק חדש ונפרד לגמרי (`testEmptyBiz`, נוצר ייעודית לבדיקה הזו, אפס מוצרים/מרכיבים/אצוות) — הטאבלט מציג את הודעת "אין מוצרים" הנכונה לבעל/ת העסק. 9/9 בדיקות עברו.
13. ✅ (2026-08-04) בדיקת ביצועים אמיתית — לפי דיווח בעל הפרויקט ש"כל פעולה לוקחת הרבה זמן להיטען". נמדדו זמנים אמיתיים (לא הערכה) מול Emulator אמיתי, עם 60 אצוות פעילות ו-15 מוצרים זרועים.
    - **הממצא המרכזי לא היה קוד**: אמולטור מקומי שכבר רץ (מתהליך ישן, זמן ריצה ארוך) הפסיק להגיב לגמרי לקריאות ל-Cloud Functions — `curl` ישיר ל-`verifyStaffPin` לא קיבל תשובה אחרי 30+ שניות. אחרי הפעלה מחדש, כל הפעולות רצות תקין. **זו כנראה הסיבה המרכזית לתחושת האיטיות בפיתוח** — לא תישאר בייצור לעולם (שם רצה תשתית Cloud Functions מנוהלת, לא תהליך מקומי שיכול להיתקע). נשמר כזיכרון פרויקט (מחוץ לריפו) כדי שפעם הבאה שמשהו "מרגיש איטי" נבדוק קודם אם האמולטור בכלל מגיב, לפני שנחשוד בקוד.
    - **כל הפעולות התפעוליות עמוק מתחת ליעדי האפיון**, מול אמולטור בריא: רשימה ראשית (batches+products, במקביל) ~150ms (יעד: <2 שניות), יצירת אצווה ~70ms (יעד: כמה שניות בודדות), עדכון סטטוס ~90ms, עדכון כמות ~75ms, טעינת מסך ניהול ~30ms. אין בעיית over-fetch ברשימה הראשית — שאילתת `batches` מסננת `status==active` (לא מושכת היסטוריה), `products` מסננת `active==true`.
    - **נמצא ותוקן ממצא אמיתי**: [`WasteReport.tsx`](src/admin/WasteReport.tsx) שלף עלות מתכון (`totalCostSnapshot`) לכל שילוב מוצר/גרסת-מתכון ייחודי בלולאת `for...of` עם `await` **בתוך** הלולאה — סדרתי, לא מקבילי. נמדד בפועל: ~475ms (ריצה ראשונה) / ~311ms (ריצה חוזרת) מול ~269ms / ~116ms כשאותן קריאות רצות עם `Promise.all` — כמעט פי 2-3 יותר איטי מהנדרש, ורק היה הולך ומחמיר עם עוד מוצרים/גרסאות מתכון. תוקן: הקריאות ל-`recipeVersions` רצות עכשיו במקביל.
    - **נוסף `limit(2000)` בטוח על שאילתת טווח התאריכים** (עם `orderBy("preparedAtClient", "desc")` כדי שהתקרה תהיה משמעותית — 2000 האצוות האחרונות בטווח, לא שרירותי) — כדי שדוח על טווח ענק לא ימשוך הכל בבת אחת לזיכרון הדפדפן. **חשוב: זו לא תקרה שקטה** — דוח פחת הוא נתון כספי שבעל/ת העסק מסתמך/ת עליו (ראו "מה המערכת" למעלה, "חישוב שווי כספי מדויק"), אז אם התקרה נפגעת מוצגת אזהרה מפורשת (`warning-text`) שהסכומים המוצגים **אינם** משקפים את כל הטווח שנבחר ויש לצמצם את הטווח — לא מציגים דוח חלקי כאילו הוא מלא.
    - **תוקן bundle**: [`App.tsx`](src/App.tsx) ייבא סטטית גם את `TabletDashboard` וגם את `AdminApp` תמיד, אז כל ביקור (טאבלט או `/admin`) הוריד את **אותה** חבילת JS אחת של 1.03MB (317KB gzip) שכללה את שני הצדדים במלואם — כולל `qrcode`/`jsqr` (טאבלט בלבד) וכל מסכי הניהול (`/admin` בלבד). תוקן עם `React.lazy` + `Suspense`: `TabletApp` (טאבלט) הוצא לקובץ נפרד חדש [`src/TabletApp.tsx`](src/TabletApp.tsx) (היה מוגדר inline בתוך `App.tsx`, לא ניתן ל-lazy-import כך), ליד `admin/AdminApp.tsx` הקיים. **נמדד בפועל אחרי הפיצול**: `/admin` יורד עכשיו רק 857KB/256KB gzip (כ-19% פחות) כי הוא כבר לא מוריד קוד סריקה/הדפסה של הטאבלט. הטאבלט יורד 1,003KB/311KB gzip — שיפור שולי בלבד (כ-2%-3%), כי הטאבלט ממילא כבר צריך את רוב האפליקציה (Firebase SDK + סריקה/הדפסה) — ה-829KB/249KB gzip המשותפים (React+Firebase SDK+Auth) חייבים להישאר משותפים, שני הצדדים תלויים בהם. הרווח המבני האמיתי לטאבלט הוא future-proofing: מסכי ניהול עתידיים לא ינפחו יותר את חבילת הטאבלט.
    - **cold start אמיתי של Cloud Functions בייצור לא ניתן למדידה מקומית**: האמולטור מריץ את כל 17 הפונקציות בתהליך Node אחד מתמיד (בלי scale-to-zero), אז אין cold start מקומי בכלל אחרי עליית האמולטור. בייצור (Cloud Functions gen2 / Cloud Run) פונקציה שלא נקראה זמן-מה יכולה "להתקרר" (טיפוסי: 1-3+ שניות לקריאה הראשונה) — סביר שיקרה בפועל בעסק פיילוט בודד עם שימוש פרצי (משמרות). לא תוקן בשלב הזה (מניעה מלאה עולה כסף — `minInstances` תמיד-פעיל) — רק דווח כציפייה סבירה, ראו "מה עדיין חסר" למטה.
    - נבדק: `test:functions` (26/26) + `test:rules` (7/7) + `npm run lint` (נקי, אזהרה קיימת אחת ב-`AuthContext.tsx` לא קשורה) + `tsc -b`+`vite build` נקיים — כל זה **אחרי** שני התיקונים, כדי לוודא שלא נשברה שום זרימה קיימת.

14. ✅ (2026-08-05) סבב תיקונים מקיף לפי סקירה ידנית של בעל הפרויקט — 14 סעיפים (מבנה מתכון, זרימת יצירת מוצר, בהירות מסכים, יצירת אצווה, ניהול עובדים, דוח חדש). תוכנן מראש כתמונה אחת (לא תיקון נקודתי לכל סעיף) — ראו תהליך התכנון והאישור בשיחה. **נבדק/הובהר לפני מימוש**: הניסוח "הטאבלט פתוח לכולם, PIN רק לפעולות מיוחדות" נבדק מול ההרשאות הדו-שכבתיות הנעולות למעלה ומול המימוש בפועל (`requireBusinessMember` חוסם כל פעולה בטאבלט ללא session מאומת) — **הוצג לבעל הפרויקט כסתירה אפשרית, והוא אישר**: מודל ההתחברות **לא השתנה** — session אחד למשמרת בדיוק כמו היום (קוד בעלים/PIN אישי בהתחברות, פתוח אח"כ לכל פעולה עד יציאה). שדה "מי הכין" (למטה) הוא שם בלבד, מנותק מזהות ה-session, **לא** דלת אחורית להסרת PIN.
    - **תפוקת מתכון (סעיפים א)** — `recipeVersions` מקבל `yieldQuantity` (כמה יוצא מהמתכון, ביחידת המוצר) ו-`costPerUnitSnapshot` (=totalCostSnapshot/yieldQuantity, מחושב ונשמר ביצירה). זה פותר את הפער שדווח בשלב 13 ("מה עדיין חסר"): עלות אצווה בפועל = `costPerUnitSnapshot * preparedQuantity`, לא `totalCostSnapshot` הגולמי בלי קשר לכמות שהוכנה. `WasteReport.tsx` עודכן לחשב לפי זה (גם `totalCost` הכולל, לא רק `wasteCost` — שניהם סבלו מאותה הנחה שגויה). `createRecipeVersion` דוחה `yieldQuantity` לא-חיובי. ראו `DATA_MODEL.md`.
    - **מניעת קפיצת מספר גרסה** — `createRecipeVersion` משווה את השורות+התפוקה המוצעות לגרסה הנוכחית לפני כתיבה; אם זהות, מחזירה `unchanged:true` על הגרסה הקיימת בלי ליצור גרסה חדשה. משולב עם `RecipeLinesEditor.tsx` (רכיב חדש, מחולץ מ-`RecipeEditorDialog`) שמאתחל את הטופס מתוך הגרסה הנוכחית (לא ריק) — כך "הסרת שורה" הופכת שימושית לתיקון טעות אמיתי, במניעת בחירת אותו מרכיב פעמיים בממשק (לא רק שגיאת שרת), ובהודעות ולידציה נפרדות לפי מקרה (אין מרכיב / יש מרכיב בלי כמות / אין תפוקה) במקום הודעה גורפת אחת.
    - **זרימת יצירת מוצר מאוחדת** — `NewProductWizard.tsx` (חדש): דיאלוג אחד עם שני שלבים (פרטי מוצר ← מתכון, דרך אותו `RecipeLinesEditor`), במקום דיאלוג יצירה ואז דיאלוג מתכון נפרד. `ProductFormDialog.tsx` צומצם לעריכת מוצר קיים בלבד (`product: Product`, לא `| null`).
    - **יצירת אצווה** — שדה חובה חדש "מי הכין" (`preparedByStaffId`/`preparedByNameSnapshot` על האצווה, נפרד מ-`createdByStaffId`/`createdByRole` שהם זהות ה-session), נבחר מרשימת עובדים (`listActiveStaffNames` הקיימת, כבר unauthenticated-safe) עם ברירת מחדל אוטומטית לפי מי שמחובר/ת כרגע (כולל אופציית "בעל/ת העסק" כש-owner) — לא דורש PIN נוסף. תצוגת כמויות צפויות חיה לפי המתכון (Cloud Function חדשה `getRecipePreview`, מחזירה רק `{ingredientNameSnapshot, unit, perUnitQuantity}` בלי אף שדה כספי, כדי ש-shiftManager יראה כמויות בלי לחשוף מחיר/עלות — אותה תבנית כמו `listActiveStaffNames`).
    - **ניהול עובדים מהטאבלט** — כשרשימת העובדים ריקה במסך ההתחברות, מוצג כפתור שמפנה להתחברות כבעל/ת העסק (לא נשאר סתום כמו קודם). לאחר התחברות כ-owner, `TabletDashboard` מציג כפתור "ניהול צוות" (owner-only) שפותח את `TeamManagement` הקיים (כבר owner-only דרך `setStaffPin`) כדיאלוג — **שונה מהתכנון המקורי** שדימיין טופס inline+sign-out אוטומטי בתוך `LoginScreen` עצמו; זה לא עבד בפועל כי `TabletApp`/`AppContent` מנווט אוטומטית ל-`TabletDashboard` ברגע שיש claims (אין דרך "להתחבר בלי לנווט"), אז הפתרון בפועל משתמש בניווט הזה במקום להילחם בו.
    - **דוח פחת לפי עובד** — טבלה חדשה ב-`WasteReport.tsx` (וב-PDF, `exportWasteReportPdf.ts`): שווי פחת + מס' אצוות + פירוט לפי סיבה, מקובצת לפי מי **שהכין** את האצווה (`preparedByStaffId`), לא לפי מי שסימן אותה כמושלכת/פגה — **זו בחירת עיצוב, אושרה עם בעל הפרויקט**: הפחת נובע מאיכות ההכנה (הכנת יתר/טעות בהכנה וכו', לפי `discardReasons`), לא ממי שבמקרה עמד/ה ליד הטאבלט כשהאצווה פגה.
    - קבצים עיקריים: `functions/src/recipes/createRecipeVersion.ts`, `functions/src/recipes/getRecipePreview.ts` (חדש), `functions/src/batches/createBatch.ts`, `functions/src/lib/recipeCost.ts`, `src/components/RecipeLinesEditor.tsx` (חדש), `src/components/RecipeEditorDialog.tsx`, `src/components/NewProductWizard.tsx` (חדש), `src/components/ProductFormDialog.tsx`, `src/components/CreateBatchDialog.tsx`, `src/screens/LoginScreen.tsx`, `src/screens/TabletDashboard.tsx`, `src/admin/WasteReport.tsx`, `src/admin/exportWasteReportPdf.ts`.
    - **חשוב לתיקון בעצמו, לא רק תוספת**: הבדיקה הראשונית ל"אין שינוי מהגרסה הנוכחית" השוותה רק `ingredientId+quantity+yieldQuantity` שהלקוח שולח — זה היה מפספס מקרה אמיתי: שמירה חוזרת עם אותן שורות **אחרי** שמחיר מרכיב התעדכן (הבדיקה הקיימת `createIngredient + updateIngredientPrice` בודקת בדיוק את זה) הייתה מסומנת בטעות כ"ללא שינוי" ולא יוצרת את הגרסה עם העלות המעודכנת. תוקן לפני הרצת הבדיקות: ההשוואה כוללת גם את `pricePerUnitSnapshot` המחושב (לא רק מה שהלקוח שלח), כך שרק אם גם התוכן וגם המחיר בפועל זהים — לא נוצרת גרסה חדשה.
    - נבדק בפועל (אחרי ניקוי תהליך אמולטור ישן/יתום מסשן קודם שתפס את הפורטים, ~23 שעות uptime — ראו [[project_emulator_hangs_after_long_uptime]]): `npm run lint` נקי (תוקנה גם שגיאת lint אמיתית שהוכנסה תוך כדי — ערבוב `??`/`||` בלי סוגריים ב-`IngredientFormDialog.tsx`), `functions: typecheck`+`lint` נקיים, `tsc -b && vite build` נקי, **`test:functions` 29/29** (26 קיימים + 3 חדשים: ולידציית yieldQuantity, preparedByStaffId על כל גווניו, getRecipePreview), **`test:rules` 7/7** ללא שינוי (המבנה החדש לא נגע בהרשאות/Rules).
    - **אומת בדפדפן אמיתי מקיף (Playwright, headless, מול Emulator אמיתי + `npm run dev`)**, עסק חדש ונקי (`demoBiz`, אותה תבנית כמו שלבים קודמים): יצירת 2 מרכיבים → "מוצר חדש" דרך האשף המאוחד (שדה תפוקה, מעבר אוטומטי לשלב מתכון **באותו דיאלוג**, ניסיון לבחור אותו מרכיב פעמיים חסום בממשק) → שמירה (עלות 33.00/20=1.65 ליחידה, מוצג נכון) → פתיחה חוזרת של עורך המתכון: השורות מאותחלות מהגרסה הקיימת (לא ריק), שמירה עם בדיוק אותם ערכים מחזירה "אין שינוי מהגרסה הנוכחית" **בלי** להקפיץ את מספר הגרסה, שינוי אמיתי (הסרת שורה + שינוי תפוקה) יוצר גרסה 2 כראוי. בטאבלט: staff picker ריק מציג הודעה+כפתור להתחברות כבעלים (לא מבוי סתום), התחברות כבעלים חושפת כפתור "ניהול צוות" owner-only, יצירת עובדת חדשה ("דנה") מתוכו, ואז כניסה כמנהלת משמרת (בלי כפתור "ניהול צוות" ב-session הזה). יצירת אצווה: שדה "מי הכין" מתמלא אוטומטית לזהות המחוברת ("דנה (אני)"), ותצוגת כמויות צפויות חיה מציגה בדיוק `2.00 kg חציל` (5÷25×10, לפי גרסה 2 המעודכנת, בלי המרכיב שהוסר). עדכון כמות חלקי (10→6) + השלכה (6, "הכנת יתר") → דוח הפחת מציג שווי כולל 10.00 ושווי פחת 6.00 (60%, בדיוק לפי `costPerUnitSnapshot(1.00) * preparedQuantity(10) * wasteRatio(0.6)`), וטבלת "פחת לפי עובד" מייחסת את זה נכון ל"דנה" עם הסיבה "הכנת יתר". 22/22 בדיקות עברו בזרימה המשולבת (מרכיבים→אשף מוצר→מתכון→טאבלט→אצווה→פחת חלקי); כניסת Google *שנייה* לדוח הפחת נכשלה שם מתקלת Playwright (popup לא נתפס ב-context ישן אחרי שימוש ממושך) — לא קשור לקוד האפליקציה, אומת בנפרד ב-context נקי (סקריפט מבודד, נתוני האמולטור מההרצה הקודמת) ועבר: שווי כולל/פחת/פילוח לפי עובד מדויקים.

בסיום כל שלב: להריץ בפועל ולוודא שזה עובד (לא רק לכתוב קוד), ולדווח מה נבנה / אילו קבצים / איך בודקים / אילו בדיקות רצו בפועל / מה פתוח — לפני מעבר לשלב הבא.

## מה עדיין חסר כדי להריץ בפועל

- פרויקט Firebase אמיתי (dev + prod) — `.firebaserc` ו-`.env.example` מכילים placeholders בלבד. אין להמציא ערכים.
- דגם טאבלט אנדרואיד ומדפסת מדבקות תרמית — טרם נרכשו. `WebBluetoothPrinterAdapter` הוא שלד שממתין לדגם; `BrowserPrintAdapter` (דיאלוג הדפסה של הדפדפן) עובד כבר עכשיו כברירת מחדל.
- **אמינות ה-Scheduled Function (`checkExpiringBatches`) בייצור אמיתי — עדיין לא מאומתת, ראו שלב 9 למעלה.** לפני שאפשר להכריז עליה כאמינה: (1) פרויקט Firebase אמיתי **חייב תוכנית Blaze** (pay-as-you-go) — Scheduled Functions (Cloud Scheduler + Pub/Sub) לא זמינות בתוכנית Spark החינמית; (2) לפרוס בפועל (`firebase deploy --only functions`) ולוודא ב-Google Cloud Console (Cloud Scheduler) שנוצרה עבודה מתוזמנת, מופעלת (Enabled), וה-IAM שלה תקין; (3) להריץ אותה בפועל לפחות כמה פעמים אמיתיות (או להריץ "Run Now" ידנית בקונסולה) ולבדוק ב-Cloud Functions Logs שאין שגיאות; (4) לחווט התראה בסיסית (Cloud Monitoring alert / Error Reporting) שתתריע אם ההרצה המתוזמנת מתחילה להיכשל בשקט — זו מנגנון הבטיחות המרכזי של המערכת, ולכן קריטי שכשל בו לא יעבור בלי ששמים לב.
- ~~דיוק כספי בדוח הפחת (שלב 7) תלוי בהחלטת מוצר פתוחה: האם עלות מתכון מייצגת "הרצה אחת"...~~ **נפתר בשלב 14**: `recipeVersions.yieldQuantity` + `costPerUnitSnapshot`, עלות אצווה = `costPerUnitSnapshot * preparedQuantity`.
- ~~"תדירות עדכון כמות בשימוש חלקי" ל-"כל סוף יום" — לא מומש~~ **תוקן בשלב 12** (`updateBatchQuantity` + UI + תזכורת ברמת המוצר). **נשאר פתוח:** ברירת המחדל *ברמת העסק* (`settings.defaultPartialUsageUpdateFrequency`) עדיין לא נקראת — התזכורת עובדת רק כשמוצר ספציפי מוגדר במפורש ל-`endOfDay`, ראו שלב 12 ו-`DATA_MODEL.md`.
- ארכוב טכני של אצוות מגיל שנה (ראו שלב 11) — לא קיים, עדיפות נמוכה (לא רלוונטי בפועל עד שיהיו אצוות בנות שנה).
- **Cold start אמיתי של Cloud Functions בייצור (ראו שלב 13) — לא ניתן למדידה מקומית ולא טופל.** ב-scale-to-zero (Cloud Functions gen2/Cloud Run) הקריאה הראשונה לפונקציה אחרי חוסר פעילות יכולה לקחת 1-3+ שניות, לעומת עשרות מילישניות שנמדדו כאן מול Emulator (שמריץ הכל בתהליך אחד תמידי, בלי cold start בכלל). בעסק פיילוט בודד עם שימוש פרצי (משמרות) זה סביר שיורגש בפעולה הראשונה בתחילת משמרת. פתרון מלא (`minInstances`) עולה כסף תמיד-פעיל — לא מומלץ להזמין לפני שיש נתונים אמיתיים מהפיילוט שמראים שזו בעיה בפועל.
