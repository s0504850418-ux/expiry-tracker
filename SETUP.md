# הקמה והרצה מקומית — מדריך למי שלא פיתח את הפרויקט

מדריך הזה מיועד למי שקיבל/ה את הפרויקט הזה כדי להריץ אותו על המחשב שלו/ה (Windows), **בלי** צורך להבין קוד. כל שלב כולל בדיוק מה להקליד או להעתיק — אין צורך להחליט כלום בעצמכם.

בסוף המדריך תהיה לכם מערכת שלמה רצה על המחשב שלכם: מסך "טאבלט" (יצירת אצוות, סימון סטטוס) ומסך "ניהול" (דוחות, מוצרים, צוות) — עם עסק דמו שכבר מוכן עם מוצרים ומתכונים, כדי שאפשר יהיה להתחיל מיד בלי להגדיר שום דבר.

חשוב להבין: זו **סביבת פיתוח מקומית** — הכל רץ על המחשב שלכם בלבד (לא מחובר לאינטרנט/לענן), כולל "התחברות עם Google" שהיא סימולציה מקומית ולא Google אמיתי. שום דבר כאן לא "אמיתי" בייצור.

**דרישות מוקדמות** (לפי מה שסוכם — אמורות כבר להיות מותקנות אצלכם): Node.js, Java, ו-`firebase-tools`. אפשר לוודא שהכל מותקן עם הפקודות הבאות (בטרמינל של VS Code — ראו שלב 1 למטה):

```powershell
node -v
java -version
```

אם שתי הפקודות מחזירות מספר גרסה (ולא שגיאה כמו "לא זוהתה כפקודה") — אפשר להמשיך. אם לא, ראו סעיף "Java לא נמצאת" בסוף המדריך.

---

## שלב 1: פתיחת התיקייה ב-VS Code

1. פתחו את VS Code.
2. תפריט **File → Open Folder…**
3. בחרו את התיקייה של הפרויקט (זו שמכילה בתוכה קבצים כמו `package.json`, `README.md` וכו').
4. פתחו טרמינל בתוך VS Code: תפריט **Terminal → New Terminal** (או המקש `` Ctrl+` ``).

כל הפקודות במדריך הזה מוקלדות בתוך הטרמינל הזה, בתוך VS Code.

---

## שלב 2: התקנת תלויות (חד פעמי)

הריצו את שתי הפקודות הבאות, אחת אחרי השנייה (לחכות שהראשונה תסתיים לפני שמריצים את השנייה):

```powershell
npm install
```

```powershell
npm --prefix functions install
```

כל אחת מהן יכולה לקחת כמה דקות בפעם הראשונה. זה תקין.

---

## שלב 3: יצירת קובץ `.env.local`

זהו קובץ הגדרות שהפרויקט חייב אותו כדי לרוץ. הריצו בטרמינל בדיוק את זה (מעתיקים את כל הבלוק כמות שהוא, כולל השורה הראשונה והאחרונה):

```powershell
@'
VITE_FIREBASE_API_KEY=fake-api-key
VITE_FIREBASE_AUTH_DOMAIN=demo-expiry-tracker.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=demo-expiry-tracker
VITE_FIREBASE_STORAGE_BUCKET=demo-expiry-tracker.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID="000000000000"
VITE_FIREBASE_APP_ID="1:000000000000:web:0000000000000000000000"
VITE_APP_ENV=development
VITE_BUSINESS_ID=demoBiz
VITE_USE_EMULATORS=true
'@ | Set-Content -Encoding ascii .env.local
```

הפקודה הזו יוצרת קובץ בשם `.env.local` בתיקיית הפרויקט, עם תשעת השורות שבין `@'` ל-`'@`. אין צורך לגעת בו שוב.

> אם משהו השתבש ואתם רוצים לוודא שהקובץ נוצר נכון, אפשר להריץ `Get-Content .env.local` ולוודא שמוצגות בדיוק תשעת השורות שלמעלה.

---

## שלב 4: הקמת עסק דמו (חד פעמי)

הפקודה הבאה יוצרת "עסק דמו" עם מוצרים ומתכונים מוכנים, כדי שאפשר יהיה להתחיל לעבוד מיד:

```powershell
npm run setup:demo
```

**בפעם הראשונה זה יכול לקחת כמה דקות** (מוריד קבצים של סביבת ההדמיה של Firebase, כמה מאות מגה-בייט) — כל עוד רואים פלט מתעדכן בטרמינל, זה תקין ולא תקוע. בסוף אמורה להופיע ההודעה:

```
הקמת עסק הדמו הושלמה בהצלחה. אפשר עכשיו להריץ: npm run start:all
```

הפקודה הזו צריכה לרוץ **פעם אחת בלבד** (ראו "בפעם הבאה" למטה) — היא לא מוחקת נתונים אם מריצים אותה שוב בטעות, אבל גם אין בזה צורך.

---

## שלב 5: הרצת המערכת

```powershell
npm run start:all
```

זו הפקודה שמריצים בכל פעם שרוצים לעבוד עם המערכת. היא מפעילה בבת אחת גם את שרת ההדמיה של Firebase (Auth+Firestore+Functions) וגם את האפליקציה עצמה, ומדפיסה את הפלט של שניהם יחד לאותו טרמינל (עם התוויות `[emulators]` ו-`[app]` בתחילת כל שורה).

חכו עד שרואים בטרמינל את שתי ההודעות האלה (הסדר לא משנה, לפעמים אחת מופיעה לפני השנייה):

```
[emulators] All emulators ready! It is now safe to connect your app.
[app] ➜  Local:   http://localhost:5173/
```

**את הטרמינל הזה משאירים פתוח** כל עוד רוצים שהמערכת תרוץ — הוא לא "תקוע", הוא ממשיך לרוץ ברקע כדי לשרת את האפליקציה.

עכשיו פתחו דפדפן (Chrome/Edge) בכתובת שהודפסה אחרי `Local:` (בדרך כלל `http://localhost:5173/` בדיוק — אם המספר בכתובת שונה, זה בסדר, פשוט השתמשו בכתובת שהטרמינל הדפיס בפועל).

---

## שלב 6: כניסה למערכת

### מסך הטאבלט — `http://localhost:5173/`

המסך נפתח ישר לרשימת אצוות (כרגע ריקה) — **אין צורך להתחבר בכלל** כדי לצפות או ליצור אצווה חדשה. לחצו על **"אצווה חדשה"** ותראו את שני המוצרים המוכנים מראש: "רוטב עגבניות" ו"רוטב פסטו".

לפעולות מתקדמות יותר (ניהול מוצרים/מרכיבים, ניהול צוות) צריך "להתחבר" בתוך אותו מסך, דרך הכפתור **"כניסה כמנהל/ת משמרת / בעל/ת העסק"** שבפינה השמאלית העליונה:

| מי | איך | קוד/PIN |
|---|---|---|
| בעל/ת העסק | לחצו "בעל/ת העסק" והזינו קוד מנהל | `123456` |
| מנהל/ת משמרת (עובדת בשם "דנה") | לחצו "מנהל/ת משמרת", בחרו "דנה" והזינו PIN | `4321` |

### מסך הניהול — `http://localhost:5173/admin`

זהו מסך נפרד (דוח פחת, ניהול מוצרים, יומן פעולות) שדורש "התחברות עם Google" — **אבל זו סביבת פיתוח מקומית, אז זה לא Google אמיתי**: זהו מסך הדמיה מקומי של Firebase שמאפשר להקליד כל כתובת מייל בלי סיסמה אמיתית. כך מתחברים:

1. גשו ל-`http://localhost:5173/admin` ולחצו על **"התחברות עם Google"**.
2. ייפתח חלון קופץ (popup) של הדמיית ההתחברות. אם מוצג בו מסך עם חשבונות/רשימה, לחצו על **"Add new account"**.
3. בשדה האימייל שיופיע הקלידו בדיוק:

   ```
   owner@demo.test
   ```

4. לחצו על הכפתור **"Sign in with Google.com"**.
5. החלון הקופץ ייסגר לבד ומסך הניהול ייטען אוטומטית — אין צורך ללחוץ שוב על כלום.

---

## שלב 7: סיום עבודה

כדי לעצור את המערכת: לכו לטרמינל שבו רץ `npm run start:all`, לחצו בתוכו פעם אחת (כדי לוודא שהוא בפוקוס) ואז הקישו **Ctrl+C פעם אחת**.

**חשוב:** אחרי ה-Ctrl+C תראו בטרמינל הודעות כמו `Automatically exporting data` ואז `Export complete` — זה השלב שבו הנתונים (האצוות שיצרתם, וכו') נשמרים לדיסק. **חכו לראות את ההודעות האלה** לפני שסוגרים את חלון הטרמינל/VS Code לגמרי. אם סוגרים באמצע, ייתכן שהעבודה מאותה הפעלה לא תישמר (עסק הדמו עצמו תמיד בטוח, כי הוא כבר נשמר פעם אחת בשלב 4).

---

## בפעם הבאה — איך להריץ שוב

**לא** צריך לחזור על שלבים 1–4. פותחים את VS Code, פותחים טרמינל, ומריצים רק:

```powershell
npm run start:all
```

הנתונים מהפעם הקודמת (כולל עסק הדמו וכל מה שיצרתם) נטענים אוטומטית.

---

## מה לעשות אם משהו לא עובד

### "פורט תפוס" / "port is already in use" / "EADDRINUSE"

כנראה יש כבר הרצה קודמת של `npm run start:all` שרצה ברקע (למשל טרמינל ישן שנשאר פתוח, או שהמחשב לא כובה כמו שצריך). פתרון:

1. חפשו וסגרו כל חלון טרמינל/VS Code ישן שעדיין מריץ את הפרויקט הזה.
2. אם זה לא עוזר — הפעילו מחדש את המחשב (זה תמיד מנקה תהליכים תקועים) ונסו שוב.

### האמולטור "תקוע" — הטרמינל מראה "All emulators ready" אבל שום דבר לא קורה בדפדפן, או שהכל נהיה איטי מאוד

זה קרה לנו גם בפיתוח: תהליך אמולטור שרץ הרבה זמן ברצף יכול "להיתקע" בשקט (ממשיך להיראות רץ, אבל לא מגיב בפועל). הפתרון הכי אמין: בטרמינל שרץ בו `npm run start:all`, הקישו Ctrl+C (וחכו ל"Export complete" כמו בשלב 7), ואז הריצו את `npm run start:all` מחדש. אם זה קורה הרבה — כדאי לא להשאיר את המערכת רצה ברקע ימים שלמים בלי הפסקה.

### "Java לא נמצאה" / "'java' is not recognized" / השגיאה מוזכרת ב-`npm run setup:demo` או `npm run start:all`

זה אומר ש-Java לא מותקנת, או שהיא מותקנת אבל VS Code/הטרמינל לא "רואים" אותה (בדרך כלל כי הטרמינל נפתח *לפני* שההתקנה הסתיימה). פתרון:

1. ודאו ש-Java מותקנת (JRE/JDK גרסה 11 ומעלה).
2. **סגרו את VS Code לגמרי ופתחו אותו מחדש** (לא מספיק לפתוח טרמינל חדש — לפעמים צריך את כל התהליך של VS Code מחדש כדי שיראה שינויים ב-PATH).
3. ודאו עם `java -version` בטרמינל החדש שמופיע מספר גרסה.

### מסך לבן/ריק בדפדפן, או שגיאת `ERR_CONNECTION_REFUSED`

זה אומר שהדפדפן מנסה להתחבר לכתובת שאין בה שרת רץ כרגע. בדקו לפי הסדר:

1. **האם `npm run start:all` באמת עדיין רץ?** תסתכלו על הטרמינל — אם הוא הפסיק לרוץ (חזר להיות שורת פקודה רגילה, בלי לוגים חיים), הוא נעצר. הריצו אותו שוב.
2. **האם שתי ההודעות משלב 5 (`All emulators ready` ו-`Local: http://localhost:...`) באמת הופיעו?** אם הטרמינל עדיין "בונה" או מוריד קבצים (בעיקר בהרצה הראשונה), פשוט לא הגעתם לשם עדיין — חכו.
3. **האם הכתובת בדפדפן תואמת בדיוק למה שהטרמינל הדפיס?** אם הטרמינל הדפיס פורט אחר מ-5173 (למשל כי 5173 היה תפוס), חייבים להשתמש בכתובת שהוא הדפיס בפועל, לא בכתובת מהמדריך.
4. **האם קובץ `.env.local` קיים בכלל?** בלעדיו האפליקציה זורקת שגיאה ברורה בעמוד עצמו ("משתנה סביבה חסר") ולא מסך ריק — אם אתם רואים דווקא הודעה כזו, חִזרו לשלב 3.

### שכחתי להריץ `npm run setup:demo`, או מחקתי בטעות תיקייה בשם `emulator-data`

אין בעיה — `npm run start:all` שם לב לבד שאין נתוני עסק דמו, ומריץ את ההקמה בעצמו לפני שהוא ממשיך. פשוט תנו לו לרוץ עד הסוף.

---

## הכנת קובץ ZIP לשליחה (למי ששולח/ת את הפרויקט הלאה)

**לפני שדוחסים את התיקייה ל-ZIP, יש למחוק/לא לכלול את הפריטים הבאים** (הם נוצרים מחדש אוטומטית אצל מי שמקבל את הקובץ, ולפעמים גם גדולים מדי לשליחה):

- `node_modules` (בתיקיית השורש)
- `functions/node_modules`
- `dist`
- `functions/lib`
- `emulator-data`
- `.env.local`
- `.firebase`
- `*.log` (למשל `firebase-debug.log`, `firestore-debug.log`)
- `functions/tsconfig.tsbuildinfo`
- `coverage`

כל הפריטים האלה כבר מוגדרים ב-`.gitignore` של הפרויקט, כך שאם נוח יותר — אפשר גם פשוט להעתיק את כל התיקייה למקום חדש, למחוק ידנית רק את `node_modules`/`functions/node_modules`/`dist`/`functions/lib`/`emulator-data`/`.env.local` (הכבדים/הרגישים ביותר), ואז לדחוס.

מי שמקבל/ת את ה-ZIP ממשיך/ה בדיוק מ"שלב 1" למעלה (פתיחה ב-VS Code, התקנה, יצירת `.env.local`, וכו').

---

## חלופה ל-ZIP: עבודה דרך Git (כדי שעדכונים עתידיים לא ידרשו קובץ חדש בכל פעם)

זו הדרך המומלצת אם צפויים עדכונים נוספים לפרויקט בעתיד — במקום לקבל קובץ ZIP חדש בכל פעם, פשוט "מושכים" את השינויים.

**הערה:** הריפו הזה פרטי ב-GitHub. כדי ש-`git clone` יעבוד, קודם צריך שבעל/ת הפרויקט יוסיפו אתכם כ-collaborator בהגדרות ה-repository ב-GitHub, ותצטרכו חשבון GitHub משלכם ולהתחבר איתו (הדפדפן ייפתח לבקש אימות בפעם הראשונה).

### פעם ראשונה — שכפול (Clone)

```powershell
git clone https://github.com/s0504850418-ux/expiry-tracker.git
cd expiry-tracker
git checkout feature/admin-products-and-design
```

(השורה השלישית חשובה: כל העבודה נמצאת כרגע בענף `feature/admin-products-and-design`, לא ב-`main` — אם זה משתנה בעתיד, בעל/ת הפרויקט יעדכנו איזה ענף להשתמש בו.)

לאחר מכן ממשיכים בדיוק מ"שלב 1" למעלה (פתיחה ב-VS Code בתוך תיקיית `expiry-tracker` שנוצרה, התקנה, `.env.local`, וכו').

### עדכונים עתידיים

בכל פעם שיש עדכון חדש לפרויקט:

```powershell
git pull
```

מריצים את זה מתוך תיקיית הפרויקט (ב-VS Code, אותו טרמינל). **שימו לב:** אם באותה עת גם משתנה קוד ה-Cloud Functions, `npm run start:all` בונה אותו מחדש אוטומטית בכל הרצה — אין צורך בפקודה נוספת. אם מתווספות תלויות חדשות (שינוי ב-`package.json`), יהיה צורך להריץ שוב את שלב 2 (`npm install` / `npm --prefix functions install`).
